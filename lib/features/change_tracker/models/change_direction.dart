/// Direction data moved for a tracked change.
enum ChangeDirection {
  /// Data came into the local app from another source.
  inbound,

  /// Data left the local app for another destination.
  outbound,
}
